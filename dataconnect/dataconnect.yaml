specVersion: "v1beta"
serviceId: "link-sharing-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "link-sharing-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
